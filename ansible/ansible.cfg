[defaults]
# cp-ansible's first assertion checks this ("Confirm Hash Merging Enabled").
# With it disabled the deployment halts on the very first task.
hash_behaviour = merge
host_key_checking = False
inventory = hosts.yml
vault_password_file = ~/.vault_pass
