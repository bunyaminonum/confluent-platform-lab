apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # Container-to-container over the compose network.
    url: http://prometheus:9090
    isDefault: true
    editable: false
